# .env.prod.example — exemplo para PRODUÇÃO
# Comentários em pt-BR; chaves em inglês. Não versione segredos reais.

# =========================
# App
# =========================
NODE_ENV=production
HOST=127.0.0.1
PORT=3000
TRUST_PROXY=1
SESSION_SECRET=SUBSTITUA_POR_UM_SEGREDO_FORTE_PROD

# =========================
# Banco de Dados (PostgreSQL)
# =========================
DB_DRIVER=pg
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=late_app
PGPASSWORD=SUBSTITUA
PGDATABASE=late_prod
PG_SSL=1              # use 1 quando o provedor exigir SSL (ex.: RDS)
PGSSLMODE=require     # compatibilidade com libpq

# =========================
# CORS
# =========================
CORS_ORIGINS=https://late.miahchat.com

# =========================
# Outros (e-mail opcional)
# =========================
APP_BASE_URL=https://late.miahchat.com/
MAIL_DEBUG=0
MAIL_DRIVER=smtp
SMTP_HOST=smtp.seu-provedor.com
SMTP_PORT=465
SMTP_SECURE=1
SMTP_USER=late@seu-dominio.com
SMTP_PASS=SUBSTITUA
SMTP_FROM="LATE <late@seu-dominio.com>"
