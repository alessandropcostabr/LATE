<%- include('partials/header') %>
<body class="auth-body">
  <main class="auth-layout">
    <section class="auth-card" aria-labelledby="resetTitle">
      <div class="auth-logo">
        <img src="/assets/logo.png" alt="LATE">
      </div>

      <h1 id="resetTitle" class="auth-title">Redefinir senha</h1>
      <p class="auth-subtitle">
        Crie uma nova senha seguindo as orientações de segurança.
      </p>

      <div id="resetAlert" class="alert d-none" role="alert"></div>

      <form id="passwordResetForm" class="auth-form" novalidate data-token="<%= token %>">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <input type="hidden" id="resetToken" name="token" value="<%= token %>">

        <label for="resetPassword" class="auth-label">Nova senha</label>
        <input
          type="password"
          class="auth-input"
          id="resetPassword"
          name="password"
          minlength="8"
          required
          autocomplete="new-password"
        >

        <label for="resetConfirm" class="auth-label">Confirmar nova senha</label>
        <input
          type="password"
          class="auth-input"
          id="resetConfirm"
          name="confirm"
          minlength="8"
          required
          autocomplete="new-password"
        >

        <button type="submit" class="btn btn-primary auth-submit">Salvar nova senha</button>
      </form>

      <div class="auth-help">
        <p class="small text-muted mb-2">
          Utilize ao menos 8 caracteres, misturando letras e números e evitando sequências óbvias.
        </p>
        <a href="/login">Voltar para o login</a>
      </div>
    </section>

    <section class="auth-hero" aria-hidden="true">
      <div class="auth-hero-content">
        <h2 class="auth-hero-title">Acesso seguro ao LATE</h2>
        <p class="auth-hero-text">
          Manter a senha em dia ajuda a proteger dados sensíveis da sua equipe.
        </p>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>
